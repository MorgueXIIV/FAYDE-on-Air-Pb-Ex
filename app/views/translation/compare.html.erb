<h1>Translation Compare</h1>
<p>Available translations for:	<%= link_to @result.id, {:controller => 'conversation', :action => 'trace', :dialogueid => @result.id }, :rel => 'nofollow', :title => 'Trace' %>.</p>

<table class="trace">
 <tr id="<%= @result.id %>">

		 <td> EN </td> <td class='actor'><%= @result.showActor %> - </td> <td> <%= @result.showDialogue(false,false) %></td>
			 </tr>
				 <% @results.each do | deet | %>
					 <tr> <td><%= deet[0] %> </td><td class='actor'>
						  <%=deet[1]%>
						  - </td> <td> <%= deet[2] %></td> </tr>
				 <% end %></table>
